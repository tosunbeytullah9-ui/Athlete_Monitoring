diff --git a/dist/runtime/native/render-component.js b/dist/runtime/native/render-component.js
index eba25ece1cf3fe3170cd55134df6b70c7aabd70e..f3899333c307dcf29c69ddfb2f57ce2e2322acec 100644
--- a/dist/runtime/native/render-component.js
+++ b/dist/runtime/native/render-component.js
@@ -118,7 +118,20 @@ function createAnimatedComponent(Component) {
     return AnimatedComponent;
 }
 function printUpgradeWarning(warning, originalProps) {
-    console.log(`CssInterop upgrade warning.\n\n${warning}.\n\nThis warning was caused by a component with the props:\n${stringify(originalProps)}\n\nIf adding or removing sibling components caused this warning you should add a unique "key" prop to your components. https://react.dev/learn/rendering-lists#keeping-list-items-in-order-with-key\n`);
+    // [patched] Do NOT deep-stringify originalProps. Its `children` reach into
+    // the React element / Fiber / navigation object graph (huge, with throwing
+    // getters), and walking it on every re-render hangs the JS thread and
+    // freezes the Fabric surface. Log a shallow key list instead.
+    let keys = "";
+    try {
+        keys = originalProps && typeof originalProps === "object"
+            ? Object.keys(originalProps).join(", ")
+            : String(originalProps);
+    }
+    catch {
+        keys = "[unreadable]";
+    }
+    console.log(`CssInterop upgrade warning.\n\n${warning}.\n\nCaused by a component with props: ${keys}.\n\nIf adding or removing sibling components caused this warning add a unique "key" prop. https://react.dev/learn/rendering-lists#keeping-list-items-in-order-with-key\n`);
 }
 function stringify(object) {
     const seen = new WeakSet();
@@ -131,8 +144,21 @@ function stringify(object) {
         }
         seen.add(value);
         const newValue = Array.isArray(value) ? [] : {};
-        for (const entry of Object.entries(value)) {
-            newValue[entry[0]] = replace(entry[0], entry[1]);
+        let entries;
+        try {
+            entries = Object.entries(value);
+        }
+        catch {
+            seen.delete(value);
+            return "[Unserializable]";
+        }
+        for (const entry of entries) {
+            try {
+                newValue[entry[0]] = replace(entry[0], entry[1]);
+            }
+            catch {
+                newValue[entry[0]] = "[Unserializable]";
+            }
         }
         seen.delete(value);
         return newValue;
