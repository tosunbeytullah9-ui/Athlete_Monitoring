// Push notifications require a development build (not Expo Go).
// This will be implemented in a later sprint with expo-dev-client.
export async function registerForPushNotifications(): Promise<string | undefined> {
  return undefined;
}
